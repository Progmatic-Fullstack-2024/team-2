generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String         @id @default(cuid())
  lastName     String
  firstName    String
  email        String         @unique()
  phone        String?
  password     String
  role         String
}